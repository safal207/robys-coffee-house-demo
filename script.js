const translations = {
  tr: {
    navAbout: "Hakkımızda",
    navMenu: "Menü",
    navGallery: "Fotoğraflar",
    navReviews: "Yorumlar",
    navVisit: "Ziyaret",
    skipContent: "İçeriğe geç",
    openMenu: "Menüyü aç",
    closeMenu: "Menüyü kapat",
    heroEyebrow: "GAZİPAŞA · FRESH COFFEE POINT",
    heroTitle: "İyi kahve.<br /><em>Sakin anlar.</em>",
    heroText: "Taze kahveler, lezzetli tatlılar ve günün hızından uzaklaşabileceğiniz keyifli bir atmosfer.",
    viewMenu: "Menüyü Gör",
    getDirections: "Yol Tarifi Al",
    daily: "Her gün",
    antalya: "Antalya",
    openToday: "Bugün açık",
    aboutEyebrow: "ROBY'S HİSSİ",
    aboutTitle: "Kahveden fazlası.<br /><em>Kendinize ayırdığınız zaman.</em>",
    featureCoffeeTitle: "Taze kahve",
    featureCoffeeText: "Her fincanda dengeli tat, özenli hazırlık ve iyi çekirdeklerin sıcak aroması.",
    featureDessertTitle: "Tatlı molalar",
    featureDessertText: "Kahvenize eşlik eden tatlılar, kurabiyeler ve günün küçük mutlulukları.",
    featureAtmosphereTitle: "Sakin atmosfer",
    featureAtmosphereText: "Arkadaşlarınızla buluşmak, çalışmak ya da sadece yavaşlamak için rahat bir köşe.",
    menuEyebrow: "MENÜDEN SEÇMELER",
    menuTitle: "Her ruh hâline<br /><em>bir fincan.</em>",
    menuText: "Klasik kahvelerden ferahlatıcı soğuk içeceklere, tatlı eşlikçilerden keyifli atıştırmalıklara.",
    seeInstagram: "Instagram'da keşfet",
    hotCoffee: "Sıcak Kahveler",
    coldDrinks: "Soğuk İçecekler",
    desserts: "Tatlılar",
    dessertsText: "Günün tatlıları · Kurabiyeler · Pastalar",
    quote: "Bazen ihtiyacınız olan tek şey iyi bir kahve ve biraz sakinliktir.",
    visitEyebrow: "BİZE UĞRAYIN",
    visitTitle: "Sıradaki kahveniz<br /><em>burada bekliyor.</em>",
    addressLabel: "Adres",
    hoursLabel: "Çalışma saatleri",
    openMap: "Haritada Aç",
    footerText: "Gazipaşa'da iyi kahve ve sakin anlar."
  },
  en: {
    navAbout: "About",
    navMenu: "Menu",
    navGallery: "Photos",
    navReviews: "Reviews",
    navVisit: "Visit",
    skipContent: "Skip to content",
    openMenu: "Open menu",
    closeMenu: "Close menu",
    heroEyebrow: "GAZİPAŞA · FRESH COFFEE POINT",
    heroTitle: "Good coffee.<br /><em>Calm moments.</em>",
    heroText: "Fresh coffee, delicious desserts and a relaxing atmosphere away from the rush of the day.",
    viewMenu: "View Menu",
    getDirections: "Get Directions",
    daily: "Every day",
    antalya: "Antalya",
    openToday: "Open today",
    aboutEyebrow: "THE ROBY'S FEELING",
    aboutTitle: "More than coffee.<br /><em>Time for yourself.</em>",
    featureCoffeeTitle: "Fresh coffee",
    featureCoffeeText: "Balanced flavour, thoughtful preparation and the warm aroma of good beans in every cup.",
    featureDessertTitle: "Sweet breaks",
    featureDessertText: "Desserts, cookies and small moments of happiness to enjoy with your coffee.",
    featureAtmosphereTitle: "Calm atmosphere",
    featureAtmosphereText: "A comfortable corner to meet friends, work or simply slow down for a while.",
    menuEyebrow: "FROM THE MENU",
    menuTitle: "A cup for<br /><em>every mood.</em>",
    menuText: "From classic coffees and refreshing cold drinks to sweet treats and easy snacks.",
    seeInstagram: "Explore on Instagram",
    hotCoffee: "Hot Coffee",
    coldDrinks: "Cold Drinks",
    desserts: "Desserts",
    dessertsText: "Desserts of the day · Cookies · Cakes",
    quote: "Sometimes all you need is good coffee and a little calm.",
    visitEyebrow: "COME VISIT",
    visitTitle: "Your next coffee<br /><em>is waiting here.</em>",
    addressLabel: "Address",
    hoursLabel: "Opening hours",
    openMap: "Open in Maps",
    footerText: "Good coffee and calm moments in Gazipaşa."
  },
  ru: {
    navAbout: "О нас",
    navMenu: "Меню",
    navGallery: "Фото",
    navReviews: "Отзывы",
    navVisit: "Контакты",
    skipContent: "Перейти к содержанию",
    openMenu: "Открыть меню",
    closeMenu: "Закрыть меню",
    heroEyebrow: "ГАЗИПАША · FRESH COFFEE POINT",
    heroTitle: "Хороший кофе.<br /><em>Спокойные моменты.</em>",
    heroText: "Свежий кофе, вкусные десерты и уютная атмосфера, где можно ненадолго замедлиться.",
    viewMenu: "Посмотреть меню",
    getDirections: "Проложить маршрут",
    daily: "Каждый день",
    antalya: "Анталья",
    openToday: "Сегодня открыто",
    aboutEyebrow: "АТМОСФЕРА ROBY'S",
    aboutTitle: "Больше, чем кофе.<br /><em>Время для себя.</em>",
    featureCoffeeTitle: "Свежий кофе",
    featureCoffeeText: "Сбалансированный вкус, внимательное приготовление и тёплый аромат хороших зёрен.",
    featureDessertTitle: "Сладкие паузы",
    featureDessertText: "Десерты, печенье и маленькие радости, которые отлично дополняют кофе.",
    featureAtmosphereTitle: "Спокойная атмосфера",
    featureAtmosphereText: "Уютное место для встречи, работы или простой паузы посреди насыщенного дня.",
    menuEyebrow: "ИЗ НАШЕГО МЕНЮ",
    menuTitle: "Чашка<br /><em>под любое настроение.</em>",
    menuText: "От классического кофе и освежающих холодных напитков до десертов и лёгких закусок.",
    seeInstagram: "Смотреть в Instagram",
    hotCoffee: "Горячий кофе",
    coldDrinks: "Холодные напитки",
    desserts: "Десерты",
    dessertsText: "Десерты дня · Печенье · Торты",
    quote: "Иногда всё, что нужно, — хороший кофе и немного спокойствия.",
    visitEyebrow: "ЗАХОДИТЕ К НАМ",
    visitTitle: "Ваш следующий кофе<br /><em>уже ждёт здесь.</em>",
    addressLabel: "Адрес",
    hoursLabel: "Часы работы",
    openMap: "Открыть карту",
    footerText: "Хороший кофе и спокойные моменты в Газипаше."
  }
};

const html = document.documentElement;
const header = document.querySelector(".site-header");
const menuToggle = document.querySelector(".menu-toggle");
const navLinks = [...document.querySelectorAll(".main-nav a")];
const languageButtons = [...document.querySelectorAll(".lang-button")];
let currentLanguage = "tr";

function getStoredLanguage() {
  try {
    return localStorage.getItem("robys-language") || "tr";
  } catch {
    return "tr";
  }
}

function setStoredLanguage(language) {
  try {
    localStorage.setItem("robys-language", language);
  } catch {
    // Storage can be blocked in private mode; the page still works.
  }
}

function setMenuState(isOpen, returnFocus = false) {
  document.body.classList.toggle("menu-open", isOpen);
  menuToggle.setAttribute("aria-expanded", String(isOpen));
  const dictionary = translations[currentLanguage] || translations.tr;
  menuToggle.setAttribute("aria-label", isOpen ? dictionary.closeMenu : dictionary.openMenu);

  if (isOpen) {
    window.requestAnimationFrame(() => navLinks[0]?.focus());
  } else if (returnFocus) {
    menuToggle.focus();
  }
}

function setLanguage(language) {
  const dictionary = translations[language] || translations.tr;
  currentLanguage = translations[language] ? language : "tr";
  html.lang = currentLanguage;

  document.querySelectorAll("[data-i18n]").forEach((element) => {
    const key = element.dataset.i18n;
    if (dictionary[key]) element.textContent = dictionary[key];
  });

  document.querySelectorAll("[data-i18n-html]").forEach((element) => {
    const key = element.dataset.i18nHtml;
    if (dictionary[key]) element.innerHTML = dictionary[key];
  });

  languageButtons.forEach((button) => {
    const isActive = button.dataset.lang === currentLanguage;
    button.classList.toggle("active", isActive);
    button.setAttribute("aria-pressed", String(isActive));
  });

  menuToggle.setAttribute(
    "aria-label",
    document.body.classList.contains("menu-open") ? dictionary.closeMenu : dictionary.openMenu
  );
  setStoredLanguage(currentLanguage);
}

languageButtons.forEach((button) => {
  button.addEventListener("click", () => setLanguage(button.dataset.lang));
});

menuToggle.addEventListener("click", () => {
  setMenuState(!document.body.classList.contains("menu-open"));
});

navLinks.forEach((link) => {
  link.addEventListener("click", () => setMenuState(false));
});

document.addEventListener("keydown", (event) => {
  if (event.key === "Escape" && document.body.classList.contains("menu-open")) {
    setMenuState(false, true);
  }
});

let headerTicking = false;
window.addEventListener("scroll", () => {
  if (headerTicking) return;
  headerTicking = true;
  window.requestAnimationFrame(() => {
    header.classList.toggle("scrolled", window.scrollY > 16);
    headerTicking = false;
  });
}, { passive: true });

if ("IntersectionObserver" in window) {
  const revealObserver = new IntersectionObserver((entries, observer) => {
    entries.forEach((entry) => {
      if (!entry.isIntersecting) return;
      entry.target.classList.add("visible");
      observer.unobserve(entry.target);
    });
  }, { threshold: 0.1, rootMargin: "0px 0px -4% 0px" });

  document.querySelectorAll(".reveal").forEach((element) => revealObserver.observe(element));
} else {
  document.querySelectorAll(".reveal").forEach((element) => element.classList.add("visible"));
}

const heroImage = document.querySelector(".hero-image-wrap img");
heroImage?.addEventListener("error", () => {
  heroImage.hidden = true;
  heroImage.parentElement?.classList.add("image-fallback");
}, { once: true });

function initActiveNavigation() {
  if (!("IntersectionObserver" in window)) return;
  const targets = navLinks
    .map((link) => document.querySelector(link.getAttribute("href")))
    .filter(Boolean);

  const activeObserver = new IntersectionObserver((entries) => {
    const visible = entries
      .filter((entry) => entry.isIntersecting)
      .sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];
    if (!visible) return;

    navLinks.forEach((link) => {
      const active = link.getAttribute("href") === `#${visible.target.id}`;
      link.classList.toggle("active", active);
      if (active) link.setAttribute("aria-current", "page");
      else link.removeAttribute("aria-current");
    });
  }, { rootMargin: "-25% 0px -60% 0px", threshold: [0.01, 0.2] });

  targets.forEach((target) => activeObserver.observe(target));
}

document.addEventListener("DOMContentLoaded", initActiveNavigation, { once: true });

document.getElementById("current-year").textContent = new Date().getFullYear();
setLanguage(getStoredLanguage());

if ("serviceWorker" in navigator && location.protocol === "https:") {
  window.addEventListener("load", () => {
    navigator.serviceWorker.register("./sw.js").catch(() => {
      // Offline caching is an enhancement; failure must not affect the site.
    });
  }, { once: true });
}
