# Manual QA evidence

This directory contains the human-readable web/mobile-web test plan and the browser-assisted execution script used to reproduce the same manual user journeys consistently on desktop and Pixel 7 viewports.

The GitHub Actions workflow preserves screenshots, JSON results, Markdown reports, and the local server log as a downloadable artifact. The script does not change application state outside the browser session and does not submit real orders or payments.
