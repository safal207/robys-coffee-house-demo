# Review governance migration

The review system moved from a provider-bound gate to provider-neutral, human-maintainer authority.

Removed surfaces include the external-provider configuration, scheduled request workflow, provider evidence verifier, cooperation reporter, quota waiver, provider-specific roster entry and provider-specific route stages.

The product runtime and customer-facing behavior are outside this migration.
