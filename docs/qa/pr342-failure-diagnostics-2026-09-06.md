# PR #342: preserve failure evidence before changing acceptance

Parent head: `7e9e67f2fd643cff873a824cc94ce4b73956bdda`.
Comparison base: `0893c361f035e7c6bf6d9456ee82d8dfb14ab601`.
The adjacent JSON records local observations, not a fresh CI pass or visual approval.

## Base refresh

After closing/reopening the draft PR, GitHub exposed the newer base
`5b01276b99db719cae2fc72f29d38eb00c9953f4`. The previous clean merge-tree result
was valid only for the older base above. The new base overlaps our visual
diagnostics at `captures.push`: retain its `geometry` property as well as our
incremental diagnostic output. All seven upstream QA changes are retained;
no public runtime changes or new visual allowances are introduced by this
conflict resolution. The existing #341 review record is historical and does
not approve #342's pairing differences. The PR remains draft and unmerged.

## Repair delivered

- Contextual entry writes all measured frame samples before smoothness assertions.
  Its final diagnostics record PASS/FAIL, the error, browser version, source SHA
  and local script diff, including on failure. The 21 ms limit is unchanged.
- Visual comparison records source SHAs/diffs, browser version, document and
  target geometry, font readiness and image readiness for each completed capture.
  Both sides and partial capture progress are retained. Screenshot capture,
  thresholds, reviewed-change matching and product files are unchanged.

## Local execution

`npm run check` and `npm run verify:security` passed (287 security checks and the
secret scan). Source/generated byte parity required no runtime or manifest change.

The contextual test passed once on each revision, sequentially, with Chromium
140.0.7339.186: Day and Night medians were 16.7 ms on both. Head used the diagnostic
script over unchanged product bytes; base used its original script. This supports
runner variability as an explanation of CI's 33.27 ms Night sample, but does not
erase the failed CI run or establish a flake rate.

A temporary copy with a stricter 0 ms median limit failed as expected and preserved
18 samples plus FAIL/error state. It used Chromium136 to test persistence only;
the temporary file was removed. No production threshold changed.

Visual comparison used the workflow's Playwright1.52.0/Chromium136, pixelmatch6,
pngjs7 and all43 captures. It failed8/43: four full-menu dimension changes and
four menu-share crops. All six share-card viewport configurations retained
identical width and height between revisions, while document Y and fractional
pixel phase changed. This supports downstream rasterization effects; it is not
blanket approval of changed pixels. The retry-only CI landing difference did not
recur locally. Local font/rasterization environment differs from GitHub runners,
so local ratios and dimensions must not become CI baselines or allowances.

## Retained blockers

- Review full-menu layout changes and inspect geometry from the next CI run.
  No reviewed-change records or screenshot baselines were added.
- Contextual entry must execute again on the new exact head. A local pass is
  not a replacement for failed CI; existing routing/offline gates remain.
- Android was not executed locally. Its existing smoke loads mutable public
  Pages web bytes, not the PR web tree. Base run34008698098 and head run34010593375
  both reached terminal VISUAL_STATE_TIMEOUT. The smallest causal follow-up is
  a separate controlled web fixture comparison, retaining trust boundaries,
  all deadlines, and the public-site check as a distinct integration observation.
- No merge, deployment, human approval, reviewer impersonation or threshold waiver.

Historical visual artifact9982369016 was downloaded and its SHA-256 matched
`0f9f32ef500e216c7cb8156c5baf8f8169c9ab9a0c5895f592a982032db2a2dc`.
