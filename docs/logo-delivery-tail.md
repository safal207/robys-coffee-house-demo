# Compact wordmark delivery tail

This branch closes `state.logo-delivery-tail-open` from LS trail `CPT-ROBYS-DELIVERY-TAILS-001`.

The final review must validate the compact wordmark against the current `main` customer path, including the merged hero CTA to `menu.html#pairing-offers`.

Fresh visual evidence is required after this commit. The historical approval from the earlier base is not sufficient by itself.
