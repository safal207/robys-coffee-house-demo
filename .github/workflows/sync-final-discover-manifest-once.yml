name: Retired one-shot Discover sync

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Confirm retirement
        run: echo "The one-shot Discover sync is retired; this workflow performs no repository mutation."
