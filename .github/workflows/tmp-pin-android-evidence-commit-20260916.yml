name: Temporary exact-head Android evidence commit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired after exact-head evidence commit was materialized."
