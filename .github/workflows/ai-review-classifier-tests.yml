name: AI review classifier tests (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Retired workflow marker
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Explain replacement
        run: echo "Replaced by ai-review-classifier-cases.yml"
