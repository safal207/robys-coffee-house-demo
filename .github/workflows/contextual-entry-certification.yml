name: Contextual entry certification

on:
  pull_request:
    paths:
      - "bootstrap-v2.js"
      - "morning-entry-v2.js"
      - "day-night-entry.js"
      - "sw.js"
      - "integrity-manifest.json"
      - "src/brand/**"
      - "scripts/morning-entry-smoke.mjs"
      - "scripts/contextual-entry-smoke.mjs"
      - "scripts/contextual-sw-offline-smoke.mjs"
      - "scripts/contextual-routing-smoke.mjs"
      - "scripts/generate-integrity-manifest.mjs"
      - "scripts/verify-integrity-manifest.mjs"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/contextual-entry-certification.yml"
  push:
    branches:
      - agent/day-night-motion-v1
    paths:
      - "bootstrap-v2.js"
      - "morning-entry-v2.js"
      - "day-night-entry.js"
      - "sw.js"
      - "integrity-manifest.json"
      - "src/brand/**"
      - "scripts/morning-entry-smoke.mjs"
      - "scripts/contextual-entry-smoke.mjs"
      - "scripts/contextual-sw-offline-smoke.mjs"
      - "scripts/contextual-routing-smoke.mjs"
      - "scripts/generate-integrity-manifest.mjs"
      - "scripts/verify-integrity-manifest.mjs"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/contextual-entry-certification.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: contextual-entry-certification-${{ github.ref }}
  cancel-in-progress: true

jobs:
  certify:
    name: MOTION-CONTEXT-001 Day/Night matrix
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Install deterministic Chromium
        run: npx playwright install --with-deps chromium

      - name: Verify contextual runtime syntax
        run: |
          node --check bootstrap-v2.js
          node --check morning-entry-v2.js
          node --check day-night-entry.js
          node --check sw.js
          node --check scripts/generate-integrity-manifest.mjs
          node --check scripts/verify-integrity-manifest.mjs
          node --check scripts/contextual-entry-smoke.mjs
          node --check scripts/contextual-sw-offline-smoke.mjs
          node --check scripts/contextual-routing-smoke.mjs

      - name: Verify canonical integrity before browser evidence
        run: npm run verify:integrity

      - name: Re-run certified Morning baseline
        env:
          MORNING_ENTRY_RESULTS_DIR: ${{ github.workspace }}/visual-results/morning-entry
        run: node scripts/morning-entry-smoke.mjs

      - name: Certify Day and Night contextual variants
        env:
          CONTEXTUAL_ENTRY_RESULTS_DIR: ${{ github.workspace }}/visual-results/contextual-entry
        run: node scripts/contextual-entry-smoke.mjs

      - name: Certify automatic scene routing and history bypass
        env:
          CONTEXTUAL_ROUTING_RESULTS_DIR: ${{ github.workspace }}/visual-results/contextual-routing
        run: node scripts/contextual-routing-smoke.mjs

      - name: Certify service-worker contextual precache offline
        env:
          CONTEXTUAL_SW_RESULTS_DIR: ${{ github.workspace }}/visual-results/contextual-sw
        run: node scripts/contextual-sw-offline-smoke.mjs

      - name: Upload contextual motion evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: contextual-entry-certification-${{ github.sha }}
          path: visual-results/**
          if-no-files-found: error
          retention-days: 14
